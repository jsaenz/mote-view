<%= form_for(@mote) do |f| %>
  <% if @mote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mote.errors.count, "error") %> prohibited this mote from being saved:</h2>

      <ul>
      <% @mote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
