<%= form_for(@radio) do |f| %>
  <% if @radio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@radio.errors.count, "error") %> prohibited this radio from being saved:</h2>

      <ul>
      <% @radio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
